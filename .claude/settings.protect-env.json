{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Edit|Write|Grep|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "grep -q '[.]env' && { echo 'Política del equipo: .env está fuera de alcance' >&2; exit 2; } || exit 0"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "grep -Eq 'rm -rf|[.]env' && { echo 'Política del equipo: rm -rf y .env bloqueados' >&2; exit 2; } || exit 0"
          }
        ]
      }
    ]
  }
}
