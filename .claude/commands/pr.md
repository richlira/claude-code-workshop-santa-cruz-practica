Prepare this branch for a pull request. Fill me in during the workshop:

1. Run `npm test`.
2. If it fails: do NOT commit. List the failing tests and stop.
3. If it passes: show `git diff --stat` and propose a commit message
   (what changed and WHY, one paragraph).
4. Never push.
